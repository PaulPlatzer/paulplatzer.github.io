---
import SiteLinkButtons from './SiteLinkButtons.astro';
import { navItems } from '../data/navigation';

interface Props {
  currentPath: string;
}

const { currentPath } = Astro.props;

const visibleNav = navItems.filter((item) => item.visible);
---

<aside class="sidebar">
  <img
    class="sidebar-photo"
    src="/assets/images/paul_platzer_bandeau-1.webp"
    alt="Paul Platzer"
  />

  <p class="sidebar-title">Paul Platzer's personal research website</p>

  <SiteLinkButtons />

  <hr style="border:none; border-top:1px solid #ccc;" />

  <nav class="sidebar-nav" aria-label="Main navigation">
    {visibleNav.map((item) => {
      const isActive =
        item.href === '/'
          ? currentPath === '/'
          : currentPath.startsWith(item.href);
      return (
        <a href={item.href} class={isActive ? 'active' : undefined}>
          {item.label}
        </a>
      );
    })}
  </nav>
</aside>
